#include "..\IniParser.h"
//#include "..\External Libraries\INIReader\INIReader.cpp"

int IniParser::Parse(const std::string& FileName, IniData& Data)
{
	/*INIReader Reader(FileName);
	
	const std::set<std::string>& Sections = Reader.GetSections();
	for (const std::string& Section : Sections)
	{
		const std::set<std::string>& Fields = Reader.GetFields(Section);

		
		
	}
	return 0;*/
	return 0;
}